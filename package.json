{
  "name": "employee-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "mysql": "mysql -u root -p",
    "migrate": "mysql -u root  < db/schema.sql",
    "seed": "mysql -u root  < db/seeds.sql"
  },
  "keywords": [],
  "author": "Ruth Torres",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^7.3.3",
    "mysql2": "^2.3.0"
  }
}
